# plugin-spike (throwaway)
Test repo for an ai-tools plugin restructure. No secrets. `/src` is authored; `/dist` is generated by build-spike.sh.
Tests whether Claude/Cursor/Codex/Devin can install plugins whose bodies live under `dist/plugins/<name>`.
